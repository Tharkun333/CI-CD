image: python:3.8
stages:
  - test

unit_tests:
  tags: 
    - docker
  stage: test
  coverage: /TOTAL.*\s+(\d+\%)$/
  script:
    - pip install poetry
    - poetry install
    - poetry run pip install --upgrade tox
    - poetry run tox
    # - poetry run coverage run -m pytest
    # - poetry run coverage report
